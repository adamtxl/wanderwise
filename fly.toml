# fly.toml app configuration file generated for wanderwise-at on 2024-11-07T11:59:00-06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'wanderwise-at'
primary_region = 'ord'

[build]

[env]
  TZ = 'America/Chicago'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[services]]
  protocol = ''
  internal_port = 8080
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  ports = []
  processes = ['app']

[[vm]]
  size = 'shared-cpu-2x'
